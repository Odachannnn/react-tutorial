import { makeStyles } from "@material-ui/styles";

export default makeStyles({
    root: {
        display: "flex",
        justifyContent: "center",
        alignItems: "center",
        minHeight: "100%",
    },
});
