import { makeStyles } from "@material-ui/styles";

export default makeStyles({
    label: {
        display: "block",
        paddingBottom: 40,
    },
    button: {
        display: "flex",
        justifyContent: "center"
    },
});
